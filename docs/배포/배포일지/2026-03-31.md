# 배포일지 — 2026-03-31

| 배포ID | 날짜 | 시스템 | 배포 및 수정내용 | 반영된 시스템 |
|--------|------|--------|------------------|---------------|
| DEP-016 | 2026-03-31 | GitHub | **feat: 매출 전환 모달 레이아웃, 운영용 매출전환 시드** — `Main.jsx`/`Main.css`: 매출 전환 모달 `main-table--convert-modal`, 매입 ID 열 최소 폭·상품·수량·판매 단가 입력 넉넉하게. `server/scripts/seed-convert-sample.js`, `package.json` `seed-convert-sample`. `docs/배포/Render-운영-초기데이터.md` 절 4. | GitHub → Vercel·Render 자동 배포 |
| DEP-017 | 2026-03-31 | GitHub | **ui/docs: 매입 등록 입력 가시성 강화 + 폐기 날짜 범위 + 매뉴얼/문서 인덱스 정리** — `Main.jsx`/`Main.css`: 매입 등록 모달 수량·단가·매입일 입력칸 폭 확대(항상 입력값이 보이도록), 폐기 등록 모달 매입일 검색을 `From~To` 범위로 변경(D-7~오늘 기본). `docs/매뉴얼/운영자-사용자-매뉴얼.md`: 회원가입 절 추가, 폐기 모달 설명 최신화. `docs/README.md`: docs 폴더별 역할 인덱스 재정리. `.cursor/rules/ui-ux-input-visibility.mdc` 추가. | GitHub → Vercel·Render 자동 배포 |
| DEP-018 | 2026-03-31 | GitHub | **ui: 매입 등록 모달 가로 폭 확대(1180px)** — 모달 공통 컴포넌트에 `modalClassName` 전달을 추가하고, 매입 등록 모달에 `main-modal--purchase` 적용. `Main.css`에서 `.main-modal--purchase.main-modal--fill { width: min(1180px, 100%); }` 설정. | GitHub → Vercel·Render 자동 배포 |
| DEP-019 | 2026-03-31 | GitHub | **ui: 수금 등록 모달 개선(폭·날짜 UI·컬럼명·최대금액 검증)** — `Main.jsx`/`Main.css`: 수금 등록 모달에 `main-modal--collect` 적용(1280px), 수금일시 입력 디자인 개선, 컬럼명 `이번 수금`→`현재 수금금액` 변경. 입력값 검증으로 행별 최대 미수금(`total_amount - paid_amount`) 초과 시 차단, 입력 컴포넌트에서도 `max` 기준으로 상한 적용. | GitHub → Vercel·Render 자동 배포 |
| DEP-020 | 2026-03-31 | GitHub | **ui: 폐기 등록·환불 처리 모달 폭 확대 + 노트북 반응형 공통 적용** — `Main.jsx`: 환불/폐기 모달에 `main-modal--refund`, `main-modal--disposal` 클래스 적용. `Main.css`: 두 모달 폭 1280px 확장, `@media (max-width: 1440px)`에서 모든 모달 공통으로 뷰포트 기준 폭/높이 자동 축소(노트북 대응). | GitHub → Vercel·Render 자동 배포 |
| DEP-021 | 2026-03-31 | GitHub | **ui: 수금 등록 모달 정렬/폭 추가 조정** — `Main.css`: 수금 모달 폭을 1360px로 확대, 상단 `거래처`·`수금일시`를 동일 행 그리드(`main-modal-fields--collect`)로 정렬, 매출일 셀(`main-collect-datetime-cell`)을 다른 컬럼과 수직 중앙 정렬되도록 조정. 노트북 구간에서는 자동 1열 반응형 유지. | GitHub → Vercel·Render 자동 배포 |
| DEP-022 | 2026-03-31 | GitHub | **docs: 매뉴얼 실제 PNG 캡처 업로드** — `docs/매뉴얼/images/`에 매입/매출(수금·환불)/수금정보/폐기/상품마스터 실캡처 PNG 추가(`02~06`). 기존 SVG 자리표시와 병행 사용 가능하도록 문서 자산 반영. | GitHub → Vercel·Render 자동 배포 |
