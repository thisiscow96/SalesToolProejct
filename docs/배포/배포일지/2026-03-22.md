# 배포일지 — 2026-03-22

커밋 `e4b6db1` 기준 (Cursor/에이전트 작업 후 GitHub 푸시). **배포일지 본 항목은 이전에 누락되어 있어 사후 기록함.**

| 배포ID | 날짜 | 시스템 | 배포 및 수정내용 | 반영된 시스템 |
|--------|------|--------|------------------|---------------|
| DEP-011 | 2026-03-22 | GitHub | **feat: 거래 흐름·문서 대규모 반영** — `POST` 매입·매입→매출 전환·수금(배분)·매출 환불·폐기 API (`server/index.js`). DB `004_trade_flow_allocation_refund.sql`, `schema-full.sql` 동기화 (`purchase_allocation`, `refund`, `product_transfer` 확장, `sale.status`, `payment.entry_kind`, `account.location` 등). 프론트: 매입/매출/수금/폐기 탭 버튼·폼·API 연동 (`web/src/api.js`, `Main.jsx`). docs: `docs/배포/`·`docs/아키텍쳐 구성/도메인/` 폴더로 배포·Resend·도메인 문서 이동, `docs/테스트시나리오/` API 시나리오, 객체 설계서 보강, `deployment-log` 경로 수정. | GitHub → **origin/main** 푸시 완료. Vercel·Render 자동 배포 여부는 각 대시보드에서 확인 후 이 행 **반영된 시스템** 컬럼 보완. |
| — | (수동) | Render (DB) | 프로덕션 Postgres에 **마이그레이션 004** 미적용 시: Render Start Command에 `npm run migrate && npm start` 있으면 재배포 시 자동 적용. 없으면 셸/로컬에서 `DATABASE_URL`로 `npm run migrate` 실행. | 운영 DB에 `004` 적용 여부 확인 필요 |

---

**참고:** 배포ID `DEP-004`(2026-03-13)는 **Render 환경변수 RESEND_FROM**이며, DB 파일 `004_trade_flow_allocation_refund.sql`과는 무관함. 본 기능 배포는 **DEP-011**로 기록.
