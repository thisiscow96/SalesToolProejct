# 진척일지 — 2025-03-12 · 2025-03-13

## 어제 (연동한 내역)

- **Git · GitHub**
  - 로컬 Git 저장소 초기화, GitHub 레포(`SalesToolProejct`) 연결
  - 원격 `origin` 설정, `main` 브랜치 푸시. 인증은 Personal Access Token 사용
- **Node**
  - Node.js LTS 설치. PowerShell 실행 정책 이슈로 CMD에서 npm 사용
- **웹(React)**
  - `web/` 폴더에 Vite + React 프로젝트 생성, `localhost:5173` 실행 확인
- **백엔드 · DB**
  - `server/` 에 Express + PostgreSQL(pg) 구성
  - `.env` 로 DB 연결 (DATABASE_URL 또는 PG_* 변수). `sales_tool` DB 생성 후 연동
  - `/health` 로 DB 연결 확인

---

## 오늘 (한 내용)

- **문서 · 이력**
  - `docs/연동-이력.md` — Git/Node/백엔드/DB 연동 시행착오 정리
  - `docs/object 설계서/` — 객체 설계(01), 흐름·화면 고려(02) 정리
- **DB 설계**
  - 거래처, 상품, 상품별 일별 단가, 구매, 판매, 수금·수금 배분, 폐기, 재고, **상품 이동 이력(product_transfers)** 까지 반영
  - 폐기, 손해 판매, 묶음 수금(여러 sales에 수금 배분) 반영
  - 이동 이력: 구매처→재고, 재고→거래처, 재고→폐기, `transferred_at`(시각) 추적
- **스키마**
  - `server/schema-full.sql` — 맨 처음부터 끝까지 한 번에 실행하는 전체 스키마(DROP 포함)
  - pgAdmin에서 `sales_tool` DB에 스키마 실행 완료
- **진척일지**
  - `docs/진척일지/` 폴더 생성, 어제·오늘 작업 내역 간략 정리
