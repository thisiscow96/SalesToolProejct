# 진척일지 — 2026-03-15

## Railway 백엔드 배포 완료

- **배포 환경**
  - Railway 프로젝트에 GitHub 레포(`SalesToolProejct`) 연결
  - 백엔드 서비스: Root `server`, Start `npm start`, Variables에 `DATABASE_URL` (Postgres 연결)
  - PostgreSQL: Postgres-1sVd 서비스 사용, **Generate Domain** 으로 백엔드 공개 URL 확보
- **코드 수정**
  - `app.listen(port, '0.0.0.0', ...)` 적용, `DATABASE_URL` postgres/postgresql 시작 시에만 DB 사용, 프로덕션 Postgres SSL·Pool error 핸들링
- **확인**: 백엔드 URL 접속 시 `{"ok":true,"message":"Sales tool backend is running"}` 응답 확인

## 다음 단계 (참고)

- Railway Postgres에 `schema-full.sql` 실행 후 로그인/회원가입 API 사용 가능
- 웹(React)은 Vercel 등에 배포 시 `VITE_API_URL` 에 Railway 백엔드 URL 설정
