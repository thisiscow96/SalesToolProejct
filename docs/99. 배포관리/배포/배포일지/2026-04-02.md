# 배포일지 — 2026-04-02

| 배포ID | 날짜 | 시스템 | 배포 및 수정내용 | 반영된 시스템 |
|--------|------|--------|------------------|---------------|
| DEP-024 | 2026-04-02 | GitHub | **feat: 매입 출처(`source_name`)와 매입 거래처 분리** — `purchase.source_name` 컬럼 추가(`migrations/007_purchase_source_name.sql`). `server/index.js`: 재고·매입 목록/등록 API에 출처 반영. `web/src/pages/Main.jsx`·`Main.css`: 재고현황·매입정보·매입 등록 모달·폐기 등록 모달을 **출처** / **매입 거래처** 열로 표시·검색. Render는 Start Command에 `npm run migrate && npm start` 시 배포 시 마이그레이션 007 자동 적용. | GitHub 푸시 완료 → Vercel·Render 자동 배포(연동 시). 대시보드에서 배포 완료·헬스 확인 권장 |
