# 배포일지 — 2026-03-23

UI·문서 작업 반영. **코드 변경은 로컬/저장소 기준**이며, 실제 프로덕션 반영일은 Git 푸시·Vercel·Render 대시보드 기준으로 조정해 주세요.

| 배포ID | 날짜 | 시스템 | 배포 및 수정내용 | 반영된 시스템 |
|--------|------|--------|------------------|---------------|
| DEP-012 | 2026-03-23 | GitHub (예정) | **feat/docs: 매출·폐기·수금 UI 개선 및 운영 문서** — `web/src/pages/Main.jsx`, `Main.css`: (1) **환불 처리** 모달 — 수금 발생 매출만 표시(`paid_amount>0`), 거래처 검색·사유 통일·열 겹침 완화, 매출일에서 매출수량 보조줄 제거, 사유는 체크와 무관 입력 가능, 환불일 컬럼 제거·저장 시 당일 적용, 환불 요청수량 최소 3자리 검증. (2) **수금 등록** 모달 — 매출일 셀 가로 스크롤 제거·전체 표시, 이번 수금 입력 **숫자만·최대 9자리**. (3) **폐기 등록** 모달 — 상품 드롭다운 방식 폐기 → **재고 목록** 기준, 판매처(매입처) 검색·매입일 검색(조건 AND, 둘 다 비우면 전체), 체크 후 폐기 수량·일·사유(상품 변질/상품 폐기/직접 입력)·비고, 복제 버튼 제거. `server/index.js`: 환불 가능 목록 API 조건 `paid_amount>0` 등. **문서**: `docs/화면설계/`, `docs/매뉴얼/운영자-사용자-매뉴얼.md`, 본 배포일지. | GitHub 푸시 후 Vercel·Render 자동 배포 여부 **각 대시보드에서 확인 후 기입** |

---

## 참고

- DB 스키마 변경 없음(본 배포 구간).
- 스크린샷은 `docs/매뉴얼/images/`에 저장 후 매뉴얼 Markdown에서 링크.
