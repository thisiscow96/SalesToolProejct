# 배포일지 — 2026-03-25

파일 전송 테스트 정리 및 운영 반영용 Git 푸시.

| 배포ID | 날짜 | 시스템 | 배포 및 수정내용 | 반영된 시스템 |
|--------|------|--------|------------------|---------------|
| DEP-013 | 2026-03-25 | GitHub | **chore: Salesforce 파일 전송 Chunk 탭·API 제거, 업로드 한도 조정, 전송 UI 단순화** — `web`: Chunk 테스트 탭·클라이언트 제거, `api.js` 청크 API 함수 제거, 파일 전송 테스트에서 100MB 클라이언트 제한 해제, 진행률 바 대신「전송 진행 중…」문구만 표시, `Main.css` 청크 로그·진행률 스타일 정리. `server/index.js`: Chunk 라우트·세션 맵 제거, ContentVersion 단건 업로드 100MB 서버 검사 제거, multer `fileSize` 상한 2GB. | GitHub 푸시 후 **Vercel·Render 자동 배포 연결 시** 각 대시보드에서 배포 완료 확인 |
| DEP-014 | 2026-03-25 | GitHub | **fix(api): HTTP 500 시 `message` 누락 보완** — 재고·매입·매출·수금·거래처·상품·폐기 목록 등에서 `{ ok: false }`만 반환하던 구간에 `err.message` 포함. 휴대폰/중매인 중복 확인, 이메일 인증 확인 동일. 운영에서 500 원인을 UI·클라이언트에서 구체적으로 볼 수 있음. | GitHub → Render·Vercel 자동 배포 |
| DEP-015 | 2026-03-25 | GitHub | **docs: 운영 Salesforce·초기 데이터 가이드** — `docs/환경 변수설정/README.md`: Render용 `SF_ACCESS_TOKEN`·`SF_INSTANCE_URL`(예시 URL 포함)·Password flow 변수 표. `docs/배포/Render-운영-초기데이터.md`: Render Shell에서 `npm run seed-admin` 1회 실행 방법. **토큰은 Git에 포함하지 않음.** | GitHub → 문서만. **Render Environment·Shell 작업은 수동** |

---

## 참고

- DB 마이그레이션 변경 없음.
- Salesforce 연동은 Render **Environment** (`SF_*`) 유지 필요.
