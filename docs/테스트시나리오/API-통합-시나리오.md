# API 통합 테스트 시나리오

백엔드(`server/index.js`) 기준으로, **샘플 API 호출**로 정상 동작을 검증할 때 따를 수 있는 시나리오입니다.  
(실제 자동 테스트 코드는 이 문서의 **시나리오 ID**를 그대로 테스트 이름에 쓰는 것을 권장합니다.)

---

## 0. 공통 전제

| 항목 | 내용 |
|------|------|
| DB | `schema-full.sql` 적용 또는 `001`~`004` 마이그레이션 완료 |
| 서버 | `npm start` 등으로 API 기동 (예: `http://localhost:3000`) |
| 인증 | 보호 API는 헤더 `X-Agent-No: {활성 사용자의 agent_no}` 필수 |
| Content-Type | `application/json` (POST 본문 사용 시) |

**시드/준비 데이터(권장)**  
시나리오를 한 줄로 끝까지 돌리려면 다음이 있으면 좋습니다.

- `user`: `status=active`, 알려진 `agent_no`
- `account` 2건 이상: `user_id` 동일, `type=supplier`(또는 `wholesaler` 등 매입 허용 타입) 1건, `type=customer`(또는 매출 허용 타입) 1건
- `product`: 최소 1건

> 계정·거래처가 없으면 **로그인·거래처 조회** 시나리오는 통과하고, **매입/매출** 시나리오는 스킵하거나 실패로 기록합니다.

### 목록·상세 공통 — 시행 기록 컬럼 안내

| 컬럼 | 설명 |
|------|------|
| **기대 결과** | 시나리오가 만족해야 하는 HTTP 상태·응답·DB 변화를 한 줄로 요약 |
| **성공 여부** | 실제 시행 후 기입. 권장: `○` 성공 · `×` 실패 · `−` 스킵(전제 미충족 등) · 시행 전에는 `미시행` 또는 비움 |
| **에러 사유** | 실패(`×`) 시 응답 본문·로그·재현 조건을 짧게 기록. 성공·스킵·미시행이면 `—` 또는 비움 |
| **시행일자** | 테스트 수행한 날짜 `YYYY-MM-DD`. 미시행이면 `—` 또는 비움 |

---

## 1. 시나리오 목록 (요약)

> 아래 표는 **시행할 때마다** 성공 여부·에러 사유·시행일자를 갱신합니다. (템플릿 기본값: 성공 여부 `미시행`, 에러 사유·시행일자 `—`)

| ID | 구분 | 한 줄 설명 | 기대 결과 | 성공 여부 | 에러 사유 | 시행일자 |
|----|------|------------|-----------|-----------|-----------|----------|
| S-001 | 헬스 | DB 연결 확인 | `GET /health` → 200, `ok: true`, DB 연결 시 `db: "connected"` | 미시행 | — | — |
| S-002 | 인증 | `X-Agent-No` 없이 보호 API → 401 | 보호 API 호출 시 401, 로그인 필요 메시지 | 미시행 | — | — |
| S-003 | 인증 | 잘못된 `X-Agent-No` → 401 | 존재하지 않는 번호로 401 | 미시행 | — | — |
| S-004 | 조회 | 거래처 목록 조회 성공 | 200, `data`에 본인 `account` 목록 | 미시행 | — | — |
| S-005 | 조회 | 상품 목록 조회 성공 | 200, `data` 배열 | 미시행 | — | — |
| S-006 | 조회 | 재고 조회 성공 | 200, `data` 배열(정책상 수량>0만 노출 가능) | 미시행 | — | — |
| S-007 | 매입 | 매입 등록 성공 → 재고 증가 | 201, 매입 id 반환, 재고 수량 증가·이동 이력 반영 | 미시행 | — | — |
| S-008 | 매입 | 매입 불가 거래처로 등록 시 거절 | 400, 매입 불가(타입) 메시지 | 미시행 | — | — |
| S-009 | 매입 | 필수값 누락 시 거절 | 400, 입력 검증 메시지 | 미시행 | — | — |
| S-010 | 조회 | 매입 목록에 배분·잔여 수량 필드 | 각 행에 `allocated_qty`, `remaining_qty` (004 적용 DB) | 미시행 | — | — |
| S-011 | 매출전환 | 매입→매출 전환 성공 | 201, 매출·배분·재고 감소·이동 이력 | 미시행 | — | — |
| S-012 | 매출전환 | 매입 잔여 수량 초과 시 거절 | 오류 응답, DB 롤백·데이터 불변 | 미시행 | — | — |
| S-013 | 매출전환 | 재고 부족 시 거절 | 400/500, 재고 부족 메시지 | 미시행 | — | — |
| S-014 | 조회 | 매출 목록에 상태·환불 수량 | `status`, `refunded_qty` 등 포함 | 미시행 | — | — |
| S-015 | 수금 | 수금 등록·배분 성공 | 201, `paid_amount`·`payment_status` 갱신 | 미시행 | — | — |
| S-016 | 수금 | 배분 합계 ≠ 수금액 시 거절 | 400, 합계 불일치 메시지 | 미시행 | — | — |
| S-017 | 수금 | 거래처 불일치 매출 배분 시 거절 | 500 또는 에러 메시지 | 미시행 | — | — |
| S-018 | 환불 | 환불(반품) 성공 | 201, 재고 증가·refund·이력, 선택적 환불금 | 미시행 | — | — |
| S-019 | 환불 | 환불 수량 초과 시 거절 | 400, 최대 환불 가능 수량 안내 | 미시행 | — | — |
| S-020 | 폐기 | 폐기 등록 성공 | 201, 재고 감소·이력 | 미시행 | — | — |
| S-021 | 폐기 | 재고 부족 시 거절 | 400, 재고 부족 메시지 | 미시행 | — | — |

---

## 2. 시나리오 상세

각 시나리오 하단 **시행 기록** 표는 수동 테스트 시 채웁니다.

### S-001 — 헬스 체크

| 항목 | 내용 |
|------|------|
| **목적** | 서버·DB 연결 가능 여부 |
| **메서드·경로** | `GET /health` |
| **헤더** | 없음 |
| **기대** | `200`, `ok: true`, `db: "connected"` (DB 설정된 환경) |

| 시행 기록 | 내용 |
|-----------|------|
| **기대 결과** | 위 기대와 동일 — 헬스 엔드포인트 정상, DB 설정 시 connected |
| **성공 여부** | 미시행 |
| **에러 사유** | — |
| **시행일자** | — |

---

### S-002 — 보호 API 미인증

| 항목 | 내용 |
|------|------|
| **목적** | 로그인 헤더 없을 때 거절 |
| **메서드·경로** | `GET /api/partners` (또는 `/api/inventory`) |
| **헤더** | `X-Agent-No` 없음 |
| **기대** | `401`, 본문에 로그인 필요 메시지 |

| 시행 기록 | 내용 |
|-----------|------|
| **기대 결과** | 인증 없이는 데이터 조회 불가 |
| **성공 여부** | 미시행 |
| **에러 사유** | — |
| **시행일자** | — |

---

### S-003 — 잘못된 중매인 번호

| 항목 | 내용 |
|------|------|
| **목적** | 존재하지 않는 `agent_no` |
| **메서드·경로** | `GET /api/partners` |
| **헤더** | `X-Agent-No: NO_SUCH_AGENT_99999` |
| **기대** | `401` |

| 시행 기록 | 내용 |
|-----------|------|
| **기대 결과** | 유효하지 않은 번호는 401 |
| **성공 여부** | 미시행 |
| **에러 사유** | — |
| **시행일자** | — |

---

### S-004 — 거래처 목록

| 항목 | 내용 |
|------|------|
| **목적** | 회원 소유 거래처만 조회 |
| **메서드·경로** | `GET /api/partners` |
| **헤더** | 유효한 `X-Agent-No` |
| **기대** | `200`, `data` 배열, 항목에 `id`, `name`, `type` 등 |

| 시행 기록 | 내용 |
|-----------|------|
| **기대 결과** | 본인 소유 거래처만 목록에 포함 |
| **성공 여부** | 미시행 |
| **에러 사유** | — |
| **시행일자** | — |

---

### S-005 — 상품 목록

| 항목 | 내용 |
|------|------|
| **목적** | 상품 마스터 조회 |
| **메서드·경로** | `GET /api/products` |
| **헤더** | 없어도 됨(현재 구현 기준) |
| **기대** | `200`, `data` 배열 |

| 시행 기록 | 내용 |
|-----------|------|
| **기대 결과** | 상품 마스터 JSON 배열 반환 |
| **성공 여부** | 미시행 |
| **에러 사유** | — |
| **시행일자** | — |

---

### S-006 — 재고 목록

| 항목 | 내용 |
|------|------|
| **목적** | 로그인 사용자 기준 재고 |
| **메서드·경로** | `GET /api/inventory` |
| **헤더** | 유효한 `X-Agent-No` |
| **기대** | `200`, `data` 배열 (`quantity > 0` 만 보이는 현재 정책이면 빈 배열 가능) |

| 시행 기록 | 내용 |
|-----------|------|
| **기대 결과** | 200 및 재고 정책에 맞는 목록(없으면 빈 배열 가능) |
| **성공 여부** | 미시행 |
| **에러 사유** | — |
| **시행일자** | — |

---

### S-007 — 매입 등록 (정상)

| 항목 | 내용 |
|------|------|
| **목적** | 입고 + 재고 증가 + `product_transfer` |
| **메서드·경로** | `POST /api/purchases` |
| **헤더** | `X-Agent-No`, `Content-Type: application/json` |
| **본문 예** | `{ "partner_id": <매입가능거래처ID>, "product_id": <상품ID>, "quantity": 10, "unit_price": 1000, "purchase_date": "2026-03-13", "memo": "시나리오" }` |
| **기대** | `201`, `data.id` 존재, 이후 `GET /api/inventory`에서 해당 상품 수량 증가(또는 신규 행) |

| 시행 기록 | 내용 |
|-----------|------|
| **기대 결과** | 매입 건 생성 + 재고 증가 + 이동 이력 |
| **성공 여부** | 미시행 |
| **에러 사유** | — |
| **시행일자** | — |

---

### S-008 — 매입 등록 (거래처 타입 오류)

| 항목 | 내용 |
|------|------|
| **목적** | 판매처만 있을 때 매입 차단 |
| **본문** | S-007과 동일하되 `partner_id`를 `type=customer` 인 계정으로 |
| **기대** | `400`, 매입 불가 메시지 |

| 시행 기록 | 내용 |
|-----------|------|
| **기대 결과** | 매입 불가 타입 거래처는 400 |
| **성공 여부** | 미시행 |
| **에러 사유** | — |
| **시행일자** | — |

---

### S-009 — 매입 등록 (유효성)

| 항목 | 내용 |
|------|------|
| **목적** | 필수 필드 검증 |
| **본문** | `quantity` 누락 또는 0, 또는 `purchase_date` 누락 |
| **기대** | `400` |

| 시행 기록 | 내용 |
|-----------|------|
| **기대 결과** | 잘못된 입력은 400 |
| **성공 여부** | 미시행 |
| **에러 사유** | — |
| **시행일자** | — |

---

### S-010 — 매입 목록 (배분 필드)

| 항목 | 내용 |
|------|------|
| **목적** | `remaining_qty`, `allocated_qty` 노출 |
| **메서드·경로** | `GET /api/purchases?from_date=...&to_date=...` |
| **기대** | 각 행에 `remaining_qty`, `allocated_qty` (마이그레이션 미적용 시 쿼리 오류 가능 — 전제 점검) |

| 시행 기록 | 내용 |
|-----------|------|
| **기대 결과** | 004 적용 DB에서 필드 포함·조회 성공 |
| **성공 여부** | 미시행 |
| **에러 사유** | — |
| **시행일자** | — |

---

### S-011 — 매입 → 매출 전환 (정상)

| 항목 | 내용 |
|------|------|
| **목적** | `purchase_allocation` + 매출 + 재고 감소 |
| **전제** | S-007로 만든 매입에 `remaining_qty` ≥ 전환 수량, 재고 ≥ 전환 수량 |
| **메서드·경로** | `POST /api/purchases/convert-to-sales` |
| **본문 예** | `{ "items": [ { "purchase_id": <ID>, "partner_id": <판매가능거래처ID>, "quantity": 3, "unit_price": 1500, "sale_date": "2026-03-13" } ] }` |
| **기대** | `201`, `data`에 `sale_id`, `GET /api/sales`에 신규 행, 재고 감소, `GET /api/purchases`에서 해당 매입 `remaining_qty` 감소 |

| 시행 기록 | 내용 |
|-----------|------|
| **기대 결과** | 매출·배분·재고·잔여 수량 일관성 |
| **성공 여부** | 미시행 |
| **에러 사유** | — |
| **시행일자** | — |

---

### S-012 — 매출 전환 (잔여 초과)

| 항목 | 내용 |
|------|------|
| **목적** | 매입 잔여보다 많이 팔 수 없음 |
| **본문** | `remaining_qty`보다 큰 `quantity` |
| **기대** | `4xx/5xx`, 남은 수량 관련 메시지, 트랜잭션 롤백으로 데이터 불변 |

| 시행 기록 | 내용 |
|-----------|------|
| **기대 결과** | 오류 응답 + DB 변경 없음 |
| **성공 여부** | 미시행 |
| **에러 사유** | — |
| **시행일자** | — |

---

### S-013 — 매출 전환 (재고 부족)

| 항목 | 내용 |
|------|------|
| **목적** | 재고 원장이 부족할 때 차단 |
| **전제** | DB에서 해당 `inventory`를 인위적으로 낮춘 뒤 동일 상품 매입 잔여는 남아 있는 모순 상태(선택) |
| **기대** | `400` 또는 `500`, 재고 부족 메시지 |

| 시행 기록 | 내용 |
|-----------|------|
| **기대 결과** | 재고 부족 시 차단 |
| **성공 여부** | 미시행 |
| **에러 사유** | — |
| **시행일자** | — |

---

### S-014 — 매출 목록

| 항목 | 내용 |
|------|------|
| **목적** | 환불·상태 필드 |
| **메서드·경로** | `GET /api/sales?from_date=...&to_date=...` |
| **기대** | `status`, `refunded_qty` 등 (스키마 반영 시) |

| 시행 기록 | 내용 |
|-----------|------|
| **기대 결과** | 확장 컬럼이 응답에 포함 |
| **성공 여부** | 미시행 |
| **에러 사유** | — |
| **시행일자** | — |

---

### S-015 — 수금 등록 (정상)

| 항목 | 내용 |
|------|------|
| **목적** | `payment` + `payment_allocation` + sale `paid_amount` |
| **전제** | S-011로 만든 매출, 거래처 동일, 아직 미수 상태 |
| **메서드·경로** | `POST /api/payments` |
| **본문 예** | `{ "partner_id": <매출과동일거래처>, "amount": 4500, "paid_at": "2026-03-13", "entry_kind": "receive", "allocations": [ { "sale_id": <ID>, "amount": 4500 } ] }` |
| **기대** | `201`, `GET /api/sales` 해당 건 `paid_amount` 증가, `payment_status` 갱신 |

| 시행 기록 | 내용 |
|-----------|------|
| **기대 결과** | 수금·배분·매출 수금 상태 반영 |
| **성공 여부** | 미시행 |
| **에러 사유** | — |
| **시행일자** | — |

---

### S-016 — 수금 (배분 합계 불일치)

| 항목 | 내용 |
|------|------|
| **목적** | `amount` ≠ allocations 합 |
| **본문** | `amount: 10000`, `allocations` 합은 5000 |
| **기대** | `400` |

| 시행 기록 | 내용 |
|-----------|------|
| **기대 결과** | 400, 합계 불일치 |
| **성공 여부** | 미시행 |
| **에러 사유** | — |
| **시행일자** | — |

---

### S-017 — 수금 (거래처 불일치)

| 항목 | 내용 |
|------|------|
| **목적** | 다른 판매처 매출에 배분 불가 |
| **본문** | `partner_id`는 A, `sale_id`는 B 거래처 매출 |
| **기대** | `500` 또는 에러 메시지(현재 구현은 throw) |

| 시행 기록 | 내용 |
|-----------|------|
| **기대 결과** | 잘못된 배분은 거절 |
| **성공 여부** | 미시행 |
| **에러 사유** | — |
| **시행일자** | — |

---

### S-018 — 환불 (정상)

| 항목 | 내용 |
|------|------|
| **목적** | 반품 입고 + `refund` + 선택적 환불금 |
| **전제** | 미환불 수량이 남은 `sale` |
| **메서드·경로** | `POST /api/sales/{saleId}/refund` |
| **본문 예** | `{ "quantity": 1, "refunded_at": "2026-03-13", "reason": "시나리오", "refund_amount": 1000 }` |
| **기대** | `201`, 재고 증가, `GET /api/sales` `refunded_qty` 반영 |

| 시행 기록 | 내용 |
|-----------|------|
| **기대 결과** | 환불 건·재고·집계 필드 반영 |
| **성공 여부** | 미시행 |
| **에러 사유** | — |
| **시행일자** | — |

---

### S-019 — 환불 (수량 초과)

| 항목 | 내용 |
|------|------|
| **목적** | `sale.quantity - 이미환불` 초과 불가 |
| **본문** | 과도한 `quantity` |
| **기대** | `400` |

| 시행 기록 | 내용 |
|-----------|------|
| **기대 결과** | 400, 허용 수량 초과 불가 |
| **성공 여부** | 미시행 |
| **에러 사유** | — |
| **시행일자** | — |

---

### S-020 — 폐기 (정상)

| 항목 | 내용 |
|------|------|
| **목적** | 재고 감소 + `disposal` |
| **전제** | 폐기 수량 ≤ 현재 재고 |
| **메서드·경로** | `POST /api/disposals` |
| **본문 예** | `{ "product_id": <ID>, "quantity": 1, "disposal_date": "2026-03-13", "reason": "시나리오" }` |
| **기대** | `201`, 재고 감소 |

| 시행 기록 | 내용 |
|-----------|------|
| **기대 결과** | 폐기 등록 + 재고 차감 |
| **성공 여부** | 미시행 |
| **에러 사유** | — |
| **시행일자** | — |

---

### S-021 — 폐기 (재고 부족)

| 항목 | 내용 |
|------|------|
| **목적** | 재고보다 많이 폐기 불가 |
| **본문** | 큰 `quantity` |
| **기대** | `400`, 재고 부족 메시지 |

| 시행 기록 | 내용 |
|-----------|------|
| **기대 결과** | 400, 재고 부족 |
| **성공 여부** | 미시행 |
| **에러 사유** | — |
| **시행일자** | — |

---

## 3. 권장 실행 순서 (샘플 한 번에 검증)

한 사용자·한 상품·두 거래처(매입/매출)를 기준으로 **순서대로** 호출하면 끊김이 적습니다.

1. S-001  
2. S-002 (선택)  
3. S-004 ~ S-006 (유효 `X-Agent-No`로)  
4. S-007  
5. S-010 (매입 잔여 확인)  
6. S-011  
7. S-014 (매출 확인)  
8. S-015  
9. S-018 (부분 환불)  
10. S-020 (남은 재고 일부 폐기)  
11. S-008, S-009, S-012, S-016, S-019, S-021 (실패 케이스 샘플)

실행 후 **§1 시나리오 목록 표**의 성공 여부·에러 사유·시행일자를 일괄 갱신하는 것을 권장합니다.

---

## 4. 자동화 시 매핑 제안

| 도구 | 제안 |
|------|------|
| 파일 위치 | 예: `server/test/api.integration.test.js` 또는 `server/__tests__/api-scenarios.test.js` |
| 이름 | `describe('S-007 매입 등록')` … `it('정상')` |
| 데이터 | `.env.test` + 테스트 전용 DB 또는 트랜잭션 롤백 |

자동화 시 **성공 여부**는 테스트 통과/실패로 대체하고, **에러 사유**는 assertion 메시지·스택으로 CI 로그에 남기면 됩니다. **시행일자**는 CI 실행일 또는 빌드 번호로 대체 가능합니다.

이 문서만 먼저 유지보수하고, 테스트 코드는 시나리오 ID 주석으로 연결해 두면 **요구사항 ↔ 검증** 추적이 쉬워집니다.
